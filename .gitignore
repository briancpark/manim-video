# Build artifacts
*.o
conv/conv2d
conv/conv_filter
gemm/gemm

# Python
__pycache__/
*.pyc

# Manim renders (regenerable via `make anim*`)
**/media/

# Video pipeline intermediates (regenerable via `make short|long`)
**/video/work/

# Generated audio (a small mp3 placeholder is committed instead)
**/video/music/*.wav

# Scratch renders at a topic root (canonical output lives in <topic>/video/out/)
gemm/final*.mp4

# Downloaded music library (no-copyright, but not redistributed via git)
/music/

# Local Claude settings / OS cruft
.claude/
.DS_Store
.venv/
